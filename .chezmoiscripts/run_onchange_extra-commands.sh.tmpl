#!/bin/bash
# extra-commands SHA: {{ include "extra-commands" | sha256sum }}

set -uo pipefail

MANIFEST="${CHEZMOI_SOURCE_DIR}/extra-commands"

while IFS= read -r line || [[ -n "$line" ]]; do
  [[ -z "$line" || "$line" == \#* ]] && continue
  eval "$line" || true
done < "$MANIFEST"
